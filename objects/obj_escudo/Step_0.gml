/// @description Insert description here
// You can write your code in this editor

cor = choose(c_aqua, c_teal,c_gray);

if (instance_exists(obj_player))
{
	x = obj_player.x;
	y = obj_player.y;
}

if (image_index == 0)
	image_alpha -= 0.05;
	
	
